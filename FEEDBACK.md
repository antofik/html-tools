# Json formatter
- sometimes after scrolling system starts slowly automatically scroll both panels up, pixel-by-pixel for about 10 seconds

# Log Viewer
- no progress bar during filtering
- Filter panel should be as compact as possible. All help lines should moved into tooltips with `(?)` button. All buttons should put in same line if possible and so on.
- "Filter" button should be Green when any filters are applied, not when it is toggled open
- Add toggle button to turn on/off virtualization 

