# Diff viewer
- allow skipping lines with some patterns (e.g. keys with passwords)

# JSON formatter
- very slow on large jsons (hangs)
- show progress bar during text processing
- empty arrays show as simple [], without suggesting to expand it
- add "Paste" button to the left panel
- auto format on text paste
- make the scrolls on left and right panels work simulteniousely, acting as if single common scroll
- add line numbers
- when collapsed, show different visualization method:
    a) `{#2}` instead of current `{ {2}}`
    b) `[#2]` instead of current `[ [2]]`
- allow hiding right panel as well. When right panel is hidden, app should not take any resources calculating tree view. Left/right pannel toggles should look like groupped buttons
- all active toggle-buttons should have green border/stroke

# Timestamp converter
- very slow on large jsons (hangs)
- show progress bar during text processing
- make the scrolls on left and right panels work simulteniousely, acting as if single common scroll


